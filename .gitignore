# Private personal data - NEVER commit
private/

# Claude Code compatibility shim. Personal instructions live in private/system-instructions.md.
CLAUDE.md

# Local agent tooling and session artifacts
.claude/worktrees/
.playwright-mcp/

# Obsidian per-user state (workspace, plugin data, custom hotkeys, etc.).
# Recommended public defaults will ship under templates/obsidian-config/ in
# a later phase. For now: keep all of .obsidian/ local-only so personal
# workspace state never leaks. See README → "Using Obsidian (optional)".
.obsidian/

# Local secrets and generated development artifacts
.env
.env.*
!.env.example
.venv/
venv/
__pycache__/
.pytest_cache/
.mypy_cache/
*.pyc
*.pyo
*.zip

# OS files
.DS_Store
