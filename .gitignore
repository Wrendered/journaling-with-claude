# Private personal data - NEVER commit
private/

# Personal override files (Anthropic + Codex local-override conventions).
# These are typically symlinks to private/system-instructions.md and never tracked.
CLAUDE.md
CLAUDE.local.md
AGENTS.override.md

# Local agent tooling and session artifacts
.claude/worktrees/
.playwright-mcp/

# Local agent tooling and session artifacts
.claude/worktrees/
.playwright-mcp/

# OpenAI Codex / dual-agent setup (kept local until polished for public release)
.codex/
.agents/
AGENTS.md

# OS files
.DS_Store
